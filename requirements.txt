# Core Data Processing - Python 3.13 compatible versions
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0

# Machine Learning - Latest compatible versions
scikit-learn>=1.4.0
# tensorflow==2.15.0  # Skip for now, install later if needed
# torch==2.1.2        # Skip for now, install later if needed
# xgboost==2.0.3       # Skip for now, install later if needed

# Financial Analysis - Simplified for now
# TA-Lib==0.4.28      # Skip complex deps for now
yfinance>=0.2.18
# quantlib==1.31      # Skip complex deps for now

# Database
sqlite3  # Built-in with Python

# GUI Development
tkinter  # Built-in with Python
matplotlib==3.8.2
plotly==5.17.0

# Async Operations
asyncio  # Built-in with Python
aiohttp==3.9.1

# Utilities
requests==2.31.0
websockets==12.0
python-dotenv==1.0.0
configparser  # Built-in with Python

# Development Tools
pytest==7.4.3
black==23.12.1
flake8==7.0.0
jupyter==1.0.0

# Performance
numba==0.58.1
cython==3.0.7